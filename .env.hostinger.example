APP_NAME="Portfolio Leandro Cebreiros"
APP_ENV=production
APP_KEY=base64:OGcCqAFv2koLjnyRqofs32hL2RkqpSt9QoD2RAN7o9w=
APP_DEBUG=false
APP_URL=http://localhost # Cambia a tu dominio en Hostinger

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Base de datos (Hostinger te da host/usuario/clave específicos)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1 # Cambia al host de MySQL de Hostinger
DB_PORT=3306
DB_DATABASE=portfolio # Cambia al nombre de DB de Hostinger
DB_USERNAME=root      # Cambia al usuario de DB de Hostinger
DB_PASSWORD=lanterminO1 # Cambia a la clave de DB de Hostinger

# Evitar dependencia de DB para cada request
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
CACHE_STORE=file
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="lean.cebreiros.a@gmail.com"
MAIL_TO_ADDRESS="leancebreiros@hotmail.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Notas:
# - Sube este archivo como .env en el servidor ajustando las variables marcadas.
# - Tras subir, ejecuta (si tienes acceso): php artisan config:clear && php artisan cache:clear && php artisan view:clear
# - Asegúrate de que storage/ y bootstrap/cache/ son escribibles por PHP.
